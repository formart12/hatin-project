import 'package:flutter/material.dart';

class PlusRoutinView extends StatelessWidget {
  const PlusRoutinView({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
        onPressed: () {}, child: const Icon(Icons.abc_outlined));
  }
}
